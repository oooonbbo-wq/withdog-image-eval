# ===== 공통 유틸리티 =====
pandas>=2.0
numpy>=1.24
pillow>=10.0
scikit-learn>=1.3
tqdm>=4.66
python-dotenv>=1.0

# ===== LLM 평가기 (OpenAI GPT-4o) =====
openai>=1.0

# ===== CV 평가기 =====
# Windows에서는 PyTorch CPU 버전이 자동 설치됨.
# GPU 사용 시 https://pytorch.org/get-started/locally/ 에서 CUDA 버전 별도 설치.
opencv-python>=4.8
torch>=2.0
open-clip-torch>=2.24       # CLIP zero-shot (dog_visible, pastel, prompt_reflected 등)
ultralytics>=8.1            # YOLOv8 (dog_visible 보조)
easyocr>=1.7                # OCR (no_text)

# ===== 향후 개선 예정 (현재 미사용) =====
# retina-face>=0.0.13       # human_face_clear (tensorflow 의존)
# deeplabcut>=2.3           # 강아지 키포인트 (no_extra_legs 등)
